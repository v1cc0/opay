# docker-compose.rust.yml
# 使用 Rust MVP-A Docker 镜像部署（本地持久化 Turso 数据文件）

services:
  app:
    image: opay-rs:${IMAGE_TAG:-latest}
    ports:
      - '${RUST_APP_PORT:-8080}:8080'
    volumes:
      - ./data:/data
      - ./config.toml:/app/config.toml:ro
    restart: unless-stopped
